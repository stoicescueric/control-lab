---
title: Control & Feedback
description: Open-loop versus closed-loop control, error, disturbances, sensors, actuators, and the control-loop skeleton every later FTC controller builds on.
sidebar_position: 2
tags: [preface, feedback, closed-loop, control, ftc]
---

import Heater from '@site/src/components/simulations/foundations/Heater';

# Control & Feedback

<Difficulty level="Easy" />

<Abstract>
Control engineering is the discipline of making a physical system reach and hold a desired behavior while the real world pushes back.
Feedback is the central idea: measure the system, compare it to the target, and use that error to compute the next command.
</Abstract>

Control is not a PID acronym. PID is only one possible rule inside a much larger pattern.

Every robot mechanism has the same basic story:

1. You want something to happen.
2. The mechanism does something close, but not perfect.
3. A sensor tells you what actually happened.
4. Code decides the next motor command.

That loop is the foundation behind arm control, flywheel velocity, drivetrain heading hold, odometry
correction, path following, and state estimation.

## FTC example: an arm does not stop where power says it should

Imagine an arm that scores a game element. During testing, you discover that `0.28` motor power holds
the arm near the target angle when the battery is full and the robot is sitting still.

Then the match starts.

The battery voltage drops. The intake has a game element in it. A cable rubs slightly against the
mechanism. The same `0.28` power no longer holds the same angle. The robot did not become mysterious;
the code was open-loop. It sent a command without checking whether the command worked.

Feedback fixes that failure by turning the mechanism into a conversation:

> "Where should you be? Where are you now? How hard should I push next?"

## Open loop is not wrong; it is just blind

Open-loop control means you send an output without using the result to change the next output. A
time-based autonomous drive is open loop:

<JavaCode league="FTC" label="Open-loop drivetrain timing">{`frontLeft.setPower(0.5);
frontRight.setPower(0.5);
backLeft.setPower(0.5);
backRight.setPower(0.5);

sleep(700);

frontLeft.setPower(0.0);
frontRight.setPower(0.0);
backLeft.setPower(0.0);
backRight.setPower(0.0);`}</JavaCode>

An intake that simply runs while the driver holds a trigger is also open loop:

<JavaCode league="FTC" label="Open-loop intake">{`if (gamepad1.right_trigger > 0.2) {
    intakeMotor.setPower(1.0);
} else {
    intakeMotor.setPower(0.0);
}`}</JavaCode>

This is often fine. An intake may not need to land at a precise state. A simple timed motion may be
good enough for a low-risk autonomous action. The question is not "is open loop bad?" The question is:
what are the design requirements?

Ask:

- Does the mechanism need accuracy, or only motion?
- Will battery voltage, load, or defense contact change the result?
- Is there a sensor that can measure success?
- Is a bad final position costly during a match?

If the answer is "yes" to accuracy, disturbances, measurable success, or high cost, you probably want
feedback.

## Visual intuition

The demo below shows the difference with temperature instead of an arm, because temperature is easy to
read. The principle is identical for FTC hardware: the open-loop system keeps doing the same thing,
while the closed-loop system measures error and reacts to disturbances.

<Heater />

Watch what happens when the outside temperature changes. The open-loop room has no way to know it is
wrong. The closed-loop room sees the error grow and increases its command.

## Theoretical rigor

A feedback system starts with a target and a measurement.

$$
e = r - y
$$

Here, $e$ is error, $r$ is the reference or setpoint, and $y$ is the measured output. For an FTC arm,
$r$ might be the desired arm angle and $y$ might be the encoder-estimated arm angle.

The controller maps error into a command:

$$
u = C(e)
$$

<EquationLegend
  items={[
    {symbol: 'C(\u00b7)', meaning: 'the CONTROLLER, written as a function. Every controller in this curriculum, from bang-bang to LQR, is a different choice of C'},
  ]}
/>

Here, $u$ is the actuator command and $C(\cdot)$ is the controller rule. That rule could be as simple
as proportional control or as advanced as a model-based controller.

The robot mechanism then responds:

$$
y_{k+1} = P(y_k, u_k, d_k)
$$

<EquationLegend
  items={[
    {symbol: 'P(\u00b7)', meaning: 'the PLANT: the mechanism itself, the thing being controlled. You do not get to choose this one; physics does'},
    {symbol: 'y_k, u_k', meaning: 'the measured output and the command at loop k'},
    {symbol: 'd_k', meaning: 'DISTURBANCE: everything acting on the mechanism that you neither commanded nor modelled. A defending robot, a battery sag, a wheel on a seam'},
  ]}
/>

Here, $k$ is the current loop number, $k+1$ is the next loop, $P(\cdot)$ represents the physical plant
or mechanism, and $d_k$ is a disturbance such as battery sag, friction, defense contact, gravity, or a
game element entering the mechanism.

The key difference is whether the next command depends on the latest measurement.

| Style | Command | What it can handle |
|---|---|---|
| Open loop | $u_k = u_\text{fixed}$ | only the condition it was tuned for |
| Closed loop | $u_k = C(r_k - y_k)$ | changes in load, voltage, friction, and position |

Open loop can still be useful. Feedforward is often an open-loop prediction from a model. But by
itself, open loop cannot know when it is wrong.

Closed loop is useful because it makes failure observable. If the measured arm angle is low, the error
is positive. If the flywheel slows after shooting, the velocity error grows. If the drivetrain heading
drifts, the heading error appears. The controller does not need to guess that reality changed; the
sensor makes the change visible.

### Two first controllers

The simplest closed-loop controller is a bang-bang controller. It looks at the error and chooses one of
two outputs:

$$
u =
\begin{cases}
u_\text{up}, & e > \epsilon \\
u_\text{down}, & e < -\epsilon \\
0, & |e| \le \epsilon
\end{cases}
$$

<EquationLegend
  items={[
    {symbol: '\u03b5 (epsilon)', meaning: 'the tolerance half-width: how close counts as close enough. Inside this band the controller does nothing, which is exactly what stops it buzzing back and forth across the target'},
    {symbol: 'u_up, u_down', meaning: 'the two fixed outputs. They usually differ in magnitude, because gravity resists one direction and helps the other'},
  ]}
/>

Here, $\epsilon$ is a tolerance band. Without a tolerance, a real robot may wait forever for the
measurement to equal the setpoint exactly, which almost never happens.

<JavaCode league="FTC" label="Bang-bang arm hold with tolerance">{`static final int TOLERANCE = 20;   // encoder ticks

double error = target - measuredPosition;

if (error > TOLERANCE)       arm.setPower(0.45);    // drive up
else if (error < -TOLERANCE) arm.setPower(-0.25);   // drive down
else                         arm.setPower(0);
`}</JavaCode>

<Exercise title="Two things worth stopping on">

The two powers differ, and up is the larger one. Work out why: which direction is gravity helping, and
what would happen with 0.45 in both?

Then `TOLERANCE`, which is what makes this usable at all. Work out what the arm does with a tolerance
of zero — the error crosses back and forth by one tick, and the motor is commanded full-up, full-down,
full-up. Describe the sound, then say what it costs the battery.

<Solution>

Gravity opposes the arm going up and helps it coming down, so the same power produces very different
accelerations in the two directions. Using 0.45 both ways makes the descent a controlled fall: the arm
slams down and the driver learns not to trust it.

With zero tolerance the motor reverses at loop rate — up to 50 direction changes a second. It buzzes
audibly, the gearbox takes shock loading it was not designed for, and each reversal is a current spike
into the pack, so the rail sags for every other mechanism on the robot.

</Solution>

</Exercise>

<Exercise title="The limit you cannot tune away">

This controller has exactly three outputs. Whether the arm is 5 ticks or 500 ticks away, it applies the
same power. Work out what that does to the approach.

<Solution>

It arrives at full speed and then stops commanding — so it coasts through the target, trips the
opposite branch, and comes back. No choice of the two powers fixes it, because the controller has no
way to know it is close. That is precisely what the next controller adds.

</Solution>

</Exercise>

Bang-bang is easy to understand, but it often oscillates because the command changes abruptly. A
proportional controller is the next step:

$$
u = k_P e
$$

Here, $k_P$ is the proportional gain. A larger error creates a larger command; a smaller error creates
a smaller command.

<JavaCode league="FTC" label="Proportional arm controller">{`double error = target - measuredPosition;
double command = kP * error;

arm.setPower(clamp(command, -1, +1));
`}</JavaCode>

Three lines, and it fixes bang-bang's *discontinuity*: the further away the arm is, the harder it
pushes, and near the target it eases off by itself with no tolerance band to tune. It does not remove
overshoot — a proportional controller on something with momentum still races past the target, and
learning what to add for that is the rest of this curriculum.

<Exercise title="What kP's units are">

Work out $k_P$'s units before guessing a value. `error` is in ticks and `command` is in $[-1, 1]$, so
$k_P$ is "power per tick." If the arm is 500 ticks away and you want roughly half power, what is
$k_P$?

<Solution>

$0.5 / 500 = 0.001$ power per tick. That estimate is your starting point — a number you derived from
your own mechanism's range of travel — not a value copied out of a lesson written for someone else's
arm.

</Solution>

</Exercise>

<Exercise title="Where this one still fails">

It does fail, in three distinct ways. Match each symptom to what is missing:

- the arm stops slightly short and stays there
- the arm sags under gravity at every angle
- the arm overshoots and oscillates

<Solution>

Stopping short is missing an *integral* term: the remaining error is too small to produce enough
command to move, and nothing accumulates it. Sagging at every angle is missing *feedforward*: the
controller only reacts to error it has already allowed to happen, so gravity always wins first.
Overshoot is missing a *derivative* term: nothing damps the approach, so momentum carries the arm past
the target. Each answer is a later lesson in this module.

</Solution>

</Exercise>

This proportional rule is the first piece of PID. Later, the PID lesson adds memory of past error and
reaction to changing error, but the core feedback idea is already here.

### The loop shape

<figure className="not-prose my-7 overflow-hidden rounded-xl border border-line bg-surface shadow-card">
  <div className="bg-panel px-4 py-5">
    <svg viewBox="0 0 760 210" className="h-auto w-full" role="img" aria-label="Closed-loop feedback block diagram">
      <defs>
        <marker id="feedbackArrow" viewBox="0 0 10 10" refX="9" refY="5" markerWidth="7" markerHeight="7" orient="auto-start-reverse">
          <path d="M0 0 L10 5 L0 10 z" fill="#93a7ff" />
        </marker>
      </defs>
      <rect width="760" height="210" rx="18" fill="#0b1120" />
      <circle cx="86" cy="84" r="22" fill="#101a2e" stroke="#6f8bff" strokeWidth="2.5" />
      <text x="86" y="90" fill="#e8eefc" textAnchor="middle" fontFamily="JetBrains Mono, monospace" fontSize="14">sum</text>
      <text x="86" y="42" fill="#8294b8" textAnchor="middle" fontFamily="JetBrains Mono, monospace" fontSize="13">target r</text>
      <line x1="86" x2="86" y1="48" y2="60" stroke="#93a7ff" strokeWidth="3" markerEnd="url(#feedbackArrow)" />
      <line x1="108" x2="156" y1="84" y2="84" stroke="#93a7ff" strokeWidth="3" markerEnd="url(#feedbackArrow)" />
      <text x="132" y="72" fill="#8294b8" textAnchor="middle" fontFamily="JetBrains Mono, monospace" fontSize="12">e</text>
      <rect x="160" y="50" width="150" height="68" rx="14" fill="#16203a" stroke="#6f8bff" strokeWidth="2" />
      <text x="235" y="80" fill="#e8eefc" textAnchor="middle" fontFamily="JetBrains Mono, monospace" fontSize="15">controller</text>
      <text x="235" y="101" fill="#8294b8" textAnchor="middle" fontFamily="JetBrains Mono, monospace" fontSize="12">u = C(e)</text>
      <line x1="310" x2="370" y1="84" y2="84" stroke="#93a7ff" strokeWidth="3" markerEnd="url(#feedbackArrow)" />
      <rect x="374" y="50" width="150" height="68" rx="14" fill="#16203a" stroke="#5ce08a" strokeWidth="2" />
      <text x="449" y="80" fill="#e8eefc" textAnchor="middle" fontFamily="JetBrains Mono, monospace" fontSize="15">mechanism</text>
      <text x="449" y="101" fill="#8294b8" textAnchor="middle" fontFamily="JetBrains Mono, monospace" fontSize="12">arm, lift, drive</text>
      <line x1="524" x2="642" y1="84" y2="84" stroke="#93a7ff" strokeWidth="3" markerEnd="url(#feedbackArrow)" />
      <text x="670" y="90" fill="#e8eefc" fontFamily="JetBrains Mono, monospace" fontSize="14">output y</text>
      <path d="M610 84 L610 164 L86 164 L86 108" fill="none" stroke="#93a7ff" strokeWidth="3" markerEnd="url(#feedbackArrow)" />
      <rect x="344" y="142" width="130" height="44" rx="12" fill="#16203a" stroke="#ffc24d" strokeWidth="2" />
      <text x="409" y="169" fill="#e8eefc" textAnchor="middle" fontFamily="JetBrains Mono, monospace" fontSize="14">sensor</text>
      <text x="204" y="184" fill="#8294b8" fontFamily="JetBrains Mono, monospace" fontSize="12">measurement returns to the comparison point</text>
    </svg>
  </div>
  <figcaption className="border-t border-line bg-surface-2 px-4 py-3 text-sm leading-relaxed text-ink-soft">
    <strong className="text-ink">Closed-loop control.</strong> The sensor turns the output back into information, and that information changes the next command.
  </figcaption>
</figure>

## Engineering implementation

Production robot code should make the feedback loop explicit. Sensors read. Controllers compute.
Actuators write. Telemetry records what happened.

<JavaCode league="FTC" label="A small feedback-control architecture">{`interface FeedbackSensor {
    double getMeasurement();          // in real engineering units
}

interface Actuator {
    void setCommand(double c);        // normalized to [-1, 1]
}

interface FeedbackController {
    double calculate(double setpoint, double measurement, double dt);
}

class FeedbackLoop {
    FeedbackSensor sensor;
    FeedbackController controller;
    Actuator actuator;
    double setpoint;

    void update(double dt) {
        double measurement = sensor.getMeasurement();
        double command = controller.calculate(setpoint, measurement, dt);
        double safeCommand = clamp(command, -1, +1);
        actuator.setCommand(safeCommand);
        log(setpoint, measurement, setpoint - measurement, safeCommand);
    }
}
`}</JavaCode>

That `update()` is the block diagram from the top of this lesson, written out. Read it next to the
diagram and match each line to an arrow.

<Exercise title="What each boundary buys">

The split into three roles is the point. Work out what becomes possible at each seam:

- swap the **controller** — bang-bang becomes P becomes PID, loop unchanged
- swap the **sensor** — encoder becomes potentiometer becomes camera
- swap the **actuator** — test the whole loop with no hardware attached

Then two questions about the loop itself. Why does the clamp live in `update()` rather than inside each
controller? And why are all four values logged rather than just the command?

<Solution>

Each seam lets one piece change without the others knowing. The actuator seam is the one people
underuse: a fake actuator that just records what it was told turns the entire control loop into
something you can unit-test at your desk.

The clamp lives in the loop because otherwise every controller you ever write has to remember it, and
the one that forgets fails silently. One place, one rule.

For the log: you are tuning, the arm is oscillating, and you have one graph. If `measurement` is noisy
while `setpoint` is steady, the problem is the sensor. If `measurement` is clean but `safeCommand`
swings hard, the problem is the controller. The command alone cannot distinguish those two.

</Solution>

</Exercise>

This style makes later lessons easier to plug in. A proportional controller, PID controller,
feedforward-plus-PID controller, or motion-profiled controller can all implement the same
`FeedbackController` interface.

## Practice

Try these on a safe test stand or with the robot wheels off the ground first:

- Run a drivetrain forward for a fixed time. Does the distance change between a full battery and a
  low battery?
- Move an arm with bang-bang control and a tolerance. Does it chatter near the setpoint?
- Replace bang-bang with proportional control. What value of $k_P$ starts to oscillate?
- Add telemetry for setpoint, measurement, error, and command. Which value tells the clearest story
  when behavior is wrong?

## Hardware reality

<Admonition type="warning" title="Feedback is only as good as the measurement">

If the encoder slips, the IMU drifts, or the camera frame arrives late, the controller is reacting to
bad information. Feedback does not magically make a robot correct; it makes the robot correctable when
the measurement is trustworthy enough.

</Admonition>

<Admonition type="warning" title="Closed loop does not remove actuator limits">

If the motor is already at full power, asking for more command cannot create more voltage. Saturation
changes controller behavior, especially for integral terms, so real implementations need output
clamps, anti-windup, current limits, and carefully chosen motion targets.

</Admonition>

<Admonition type="tip" title="FTC intuition">

When a mechanism fails only after the battery drops, load changes, or a defender bumps the robot, ask
whether the code can measure the failure. If it cannot measure the failure, it cannot correct it.

</Admonition>

The next lesson, [From Math to a Loop](../software-architecture/discretization.mdx), explains how
continuous equations become the discrete update loop your Control Hub actually runs.

<Admonition type="info" title="Sources & references">

- [CTRL ALT FTC](https://www.ctrlaltftc.com/) - FTC-focused explanations of feedback control, PID, and
  feedforward that inspired this module's practical framing.
- Tyler Veness, [*Controls Engineering in FRC*](https://file.tavsys.net/control/controls-engineering-in-frc.pdf) -
  a rigorous open robotics-controls text covering feedback loops, modeling, and state-space control.
- [WPILib: Introduction to PID Control](https://docs.wpilib.org/en/stable/docs/software/advanced-controls/introduction/introduction-to-pid.html) -
  a competition-robotics reference for setpoints, measurements, error, and actuator limits.

</Admonition>
