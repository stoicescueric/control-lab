---
title: Motion Profiling
description: Don't ask a mechanism to teleport. A motion profile hands the controller a physically achievable trajectory — accelerate, cruise, decelerate — so feedforward can lead and feedback only trims.
sidebar_position: 7
tags: [motion-profiling, trapezoidal, trajectory, feedforward, control]
---

import MotionProfile from '@site/src/components/simulations/control-theory/MotionProfile';

# Motion Profiling

<Difficulty level="Hard" />

<Abstract>
A motion profile replaces an instantaneous setpoint jump with a smooth, time-parametrized trajectory the
mechanism can actually follow: ramp up at the acceleration limit, cruise at the speed limit, ramp down to
stop exactly on target. Feeding the controller this trajectory — position, velocity, and acceleration at
every instant — lets feedforward do the heavy lifting and leaves feedback only a small error to correct,
giving fast moves that don't overshoot.
</Abstract>

[PID](./pid.mdx) and [feedforward](./feedforward.mdx) answer "what command *now*?" Motion profiling answers
the question before it: *what should the target even be at this instant?* Instead of commanding the final
position and hoping, you command a moving target that respects the mechanism's real limits.

<Problem title="A step command asks for infinite acceleration">

Tell a lift "go to the top" and your controller sees the full error immediately. A proportional term
applies full power all at once, the lift jerks, the current spikes, and feedforward — which is fed the *target*
velocity — has no idea how fast you actually intend to move, so it can't help. The mechanism either
overshoots (if the gain is high) or crawls (if it's low). The setpoint asked for a jump no motor can
deliver: an instantaneous change in position means infinite velocity, an instantaneous change in velocity
means infinite acceleration. Real hardware has a top speed and a max acceleration. Plan within them.

</Problem>

<Theory title="The trapezoidal profile">

The simplest profile that respects both limits is the **trapezoid**: accelerate at the limit $a_\text{max}$
until you hit the cruise speed $v_\text{max}$, hold it, then decelerate at $a_\text{max}$ to arrive at the
target with zero velocity. Plotted against time, the velocity traces a trapezoid — hence the name.

The geometry is just kinematics. The ramp to cruise speed takes

$$
t_\text{ramp} = \frac{v_\text{max}}{a_\text{max}},
\qquad
d_\text{ramp} = \tfrac12 a_\text{max} t_\text{ramp}^2 = \frac{v_\text{max}^2}{2 a_\text{max}}
$$

<EquationLegend
  items={[
    {symbol: 't_ramp', meaning: 'how long the acceleration phase lasts, from a standstill up to cruise speed', unit: 's'},
    {symbol: 'd_ramp', meaning: 'how much DISTANCE that ramp consumes. The profile needs room for two of these, one at each end', unit: 'in'},
    {symbol: 'v_max, a_max', meaning: 'the mechanism\u2019s real limits, measured rather than assumed'},
  ]}
/>

If the move is long enough to fit both ramps ($2\,d_\text{ramp} \le d$), the middle is spent cruising for

$$
t_\text{cruise} = \frac{d - 2\,d_\text{ramp}}{v_\text{max}}
$$

<EquationLegend
  items={[
    {symbol: 'd', meaning: 'the total distance of this move', unit: 'in'},
    {symbol: 't_cruise', meaning: 'how long is spent at constant v_max, after both ramps have taken their share. If 2\u00b7d_ramp exceeds d there is no cruise phase at all and the profile becomes a triangle', unit: 's'},
  ]}
/>

If the move is **too short** to ever reach $v_\text{max}$, the trapezoid collapses to a **triangle**: you
accelerate to a lower peak and immediately decelerate, peaking at $v_\text{peak} = \sqrt{a_\text{max}\,d}$.
Drag the sliders below — shrink the distance or raise the cruise speed and watch the trapezoid become a
triangle, while the total time and peak speed update.

<MotionProfile />

For a short $5\ \mathrm{in}$ move with $a_\text{max}=20\ \mathrm{in/s^2}$ and
$v_\text{max}=10\ \mathrm{in/s}$, each full-speed ramp requires $2.5\ \mathrm{in}$. There is no
distance left to cruise, so the boundary case is triangular with
$t_\text{acc}=\sqrt{5/20}=0.5\ \mathrm{s}$ and $v_\text{peak}=10\ \mathrm{in/s}$.

At each instant the profile gives you a full target state — position $x(t)$, velocity $v(t)$, and
acceleration $a(t)$. That triplet is exactly what the rest of the stack wants: feed $v(t)$ and $a(t)$ to
[feedforward](./feedforward.mdx) ($k_V v + k_A a$) so it leads the motion, and feed $x(t)$ to
[PID](./pid.mdx) so feedback only corrects the small tracking error. The mechanism follows a plan instead
of chasing a cliff.

<Callout type="idea" title="The braking cap you already met is the deceleration ramp.">
The $v \le \sqrt{2 a d}$ speed cap from [PID-to-point](../path-following/pid-to-point.mdx) is the
*decelerating half* of this trapezoid, evaluated on the fly from the distance left. A full profile is the
same physics, planned ahead of time for the whole move instead of reacted to each loop.
</Callout>

<Callout type="note" title="Beyond the trapezoid: S-curves.">
A trapezoid's acceleration jumps instantly between $0$, $+a_\text{max}$, and $-a_\text{max}$ — those
corners are infinite <em>jerk</em>, which can make a tall mechanism wobble. An <strong>S-curve</strong>
profile rounds them by limiting jerk too, at the cost of a longer move. The trapezoid is the right default;
reach for S-curves only when jerk visibly hurts.
</Callout>

</Theory>

<Deploy title="Sample the profile each loop">

Build the profile once from your limits, then ask it for the target state at the current time every loop
and hand that to your feedforward + PID controller.

<Pseudocode label="Trapezoidal profile (build once, sample each loop)">{`TRAPEZOIDAL PROFILE
  built ONCE per move; sampled every loop afterwards

  build(distance, v_max, a_max):
      require distance is finite, and v_max > 0 and a_max > 0

      sign     <- sign(distance)          # plan in positive distance,
      distance <- | distance |            # put the sign back when sampling

      t_ramp <- v_max / a_max             # time to reach cruise speed
      d_ramp <- 0.5 * a_max * t_ramp^2    # distance used doing it

      if 2 * d_ramp >= distance:          # TRIANGLE: too short to ever cruise
          t_acc  <- sqrt(distance / a_max)
          v_peak <- a_max * t_acc
          t_flat <- 0
      else:                               # full TRAPEZOID
          t_acc  <- t_ramp
          v_peak <- v_max
          t_flat <- (distance - 2*d_ramp) / v_max

      total <- 2*t_acc + t_flat

      # Work out the triangle case yourself. If the move is too short to
      # reach v_max, the profile is accelerate-then-decelerate with no
      # cruise. Set "distance travelled accelerating + decelerating =
      # distance" and solve for t_acc. You should land on the sqrt above.

  sample(t)  ->  target position, velocity and acceleration at time t

      if t >= total:  return (sign*distance, 0, 0)     # hold the endpoint
      clamp t to at least 0

      if t < t_acc:                       # accelerating
          a <- a_max
          v <- a_max * t
          x <- 0.5 * a_max * t^2

      else if t < t_acc + t_flat:         # cruising
          a <- 0
          v <- v_peak
          x <- 0.5*a_max*t_acc^2  +  v_peak * (t - t_acc)

      else:                               # decelerating
          td <- t - t_acc - t_flat
          a  <- -a_max
          v  <- v_peak - a_max*td
          x  <- 0.5*a_max*t_acc^2 + v_peak*t_flat
                + v_peak*td - 0.5*a_max*td^2

      return (sign*x, sign*v, sign*a)

# Every x line above is just  x = x0 + v0*t + 0.5*a*t^2  applied to that
# phase. Derive the cruising and decelerating lines yourself by asking where
# the mechanism already was when the phase began.
#
# Check your implementation on a case you can verify by hand: distance 100,
# v_max 10, a_max 5. Compute t_ramp, d_ramp, decide triangle or trapezoid,
# and predict total. Then confirm sample(total/2) sits at half the distance.
`}</Pseudocode>

<Pseudocode label="Following the profile with feedforward + PID">{`each loop:
    t <- seconds since the profile started
    target_position, target_velocity, target_accel  <-  profile.sample(t)

    ff <- kS*sign(target_velocity) + kV*target_velocity + kA*target_accel
    fb <- pid(measured_position, target_position)     # correct what ff missed

    command the motor with (ff + fb) / battery_voltage

    done <- t >= profile.duration()
            and | final_target - measured_position | < TOL

# The feedforward is computed from the PROFILE, not from the measurement. It
# is the open-loop guess at the voltage this motion needs. The PID only has
# to clean up the difference, which is why its gains end up so much smaller
# than they would be alone.
#
# Work out why done checks both the clock and the position. Give a case
# where the profile has finished but the mechanism has not arrived, and say
# which of the two conditions catches it.
`}</Pseudocode>

</Deploy>

<Admonition type="warning" title="Hardware reality: profile within your *real* limits">

The profile is only honest if $v_\text{max}$ and $a_\text{max}$ are achievable. Use ~80% of the
mechanism's measured top speed and acceleration so a tired battery or extra game-piece load doesn't make
the plan impossible to follow — the moment the mechanism can't keep up, the tracking error explodes and
feedback saturates.

</Admonition>

<Admonition type="tip" title="This is what Road Runner and trajectories are">

A drivetrain **trajectory** is a motion profile run along a [path](../path-following/quintic-splines.mdx):
the spline says *where*, the profile says *how fast along it*. Road Runner and Pedro Pathing combine the
two and add feedforward + PID — exactly the stack in this module, applied in 2-D.

</Admonition>

<Admonition type="info" title="Sources & references">

- [WPILib: Trapezoidal Motion Profiles](https://docs.wpilib.org/en/stable/docs/software/advanced-controls/controllers/trapezoidal-profiles.html) -
  practical motion-profile constraints for robot mechanisms.
- Tyler Veness, [*Controls Engineering in FRC*](https://file.tavsys.net/control/controls-engineering-in-frc.pdf) -
  control-theory context for profiles, feedforward, and actuator limits.
- [Road Runner](https://rr.brott.dev/) and [Pedro Pathing](https://pedropathing.com/) - FTC libraries that
  combine path geometry, constraints, and follower/controller logic.

</Admonition>
