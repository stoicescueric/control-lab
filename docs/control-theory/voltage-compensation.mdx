---
title: Voltage Compensation
description: A robot's battery voltage sags across a match, so the same power command does less work as the game goes on. Dividing by the measured voltage holds behavior constant start to finish.
sidebar_position: 2
tags: [voltage-compensation, battery, feedforward, brownout, control]
---

import VoltageComp from '@site/src/components/simulations/control-theory/VoltageComp';

# Voltage Compensation

<Difficulty level="Easy" />

<Abstract>
A motor power command in $[-1, 1]$ delivers that fraction of the *battery's current voltage*, not a fixed
voltage — so as the pack sags over a match, the same command produces less and less. Voltage compensation
divides the intended voltage by the measured battery voltage before sending it, delivering a constant
target voltage (until the battery physically can't), which keeps feedforward gains valid and behavior
repeatable from the first second to the last.
</Abstract>

This is the shortest lesson in the module and one of the most valuable: a few characters of code that
make every other controller — [feedforward](./feedforward.mdx), [PID](./pid.mdx),
[profiles](./motion-profiling.mdx) — behave the same on a fresh pack and a tired one.

<Problem title="The same command, quietly doing less">

`motor.setPower(0.6)` does **not** apply a fixed voltage. It applies 60% of whatever the battery happens
to read right now. A freshly charged 12 V pack typically reads somewhat above nominal — on the order of
13 V — and can sag to roughly 11 V or lower under sustained load; the exact numbers depend on the battery's
age, state of charge, and the current being drawn, but this range is illustrative, not a spec. So `0.6` is
$0.6 \times 13 = 7.8$ V early in the match and $0.6 \times 11 = 6.6$ V late — a real drop in effort for the
identical command. Your auto that was tuned on a full battery
now undershoots; your flywheel that was on speed now falls short. Worse, your carefully *measured*
feedforward gains are in **volts**: and they're being applied to a command that no longer maps to volts.

</Problem>

<Theory title="Divide by what you actually have">

Every controller in this module computes a desired *voltage* — feedforward's $k_S + k_V v + k_A a$, PID's
output, a profile's command. To turn a desired voltage $V_\text{cmd}$ into the power fraction that will
actually deliver it, divide by the **measured** battery voltage:

$$
\text{power} = \frac{V_\text{cmd}}{V_\text{battery}}
$$

Now the delivered voltage is $\text{power} \times V_\text{battery} = V_\text{cmd}$ — independent of the
battery, exactly what you asked for. The only thing that can break the guarantee is physics: you cannot
deliver more than the battery holds, so once $V_\text{cmd} > V_\text{battery}$ the command saturates at
full power and the output follows the sagging pack down.

In the demo, the rose line is a raw command (delivered volts $=$ power $\times$ battery, drifting as the
pack changes); the green line is the compensated command, flat at your target until the battery can no
longer reach it. Drag the battery marker from full to sagging and watch the raw output fall away while the
compensated one holds.

<VoltageComp />

<Callout type="idea" title="It makes your gains portable in time.">
Because compensation pins the command to real volts, the feedforward constants you
[measured](./system-identification.mdx) stay correct all match — and even transfer better between two
robots with the same mechanism. Without it, every gain is implicitly "valid at whatever voltage I tuned
on," which is never the voltage you compete on.
</Callout>

</Theory>

<Deploy title="One division, or one SDK call">

Apply it at the very end, after the controller has produced a voltage. Read the battery once per loop
(reading it is not free) and clamp the result to the legal power range.

<JavaCode league="FTC" label="Voltage-compensated output">{`// A telemetry sanity floor, NOT a battery cutoff.
static final double MIN_VALID_BATTERY_VOLTS = 6.0;

void loop() {
    double vbat = voltageSensor.getVoltage();   // ONCE per loop, shared below
    setVoltage(leftMotor,  leftDesiredVolts(),  vbat);
    setVoltage(rightMotor, rightDesiredVolts(), vbat);
}

void setVoltage(DcMotor motor, double desiredVolts, double vbat) {
    if (!Double.isFinite(vbat) || !Double.isFinite(desiredVolts)
            || vbat < MIN_VALID_BATTERY_VOLTS) {
        motor.setPower(0);          // reject a disconnected sensor
        return;
    }

    double power = desiredVolts / vbat;         // volts -> the SDK's [-1, 1]
    motor.setPower(Math.max(-1, Math.min(1, power)));
}`}</JavaCode>

Every mechanism computes what it wants in volts. This is the single place volts become motor power, and
the same cached `vbat` reaches all of them.

<Exercise title="Why one reading, shared">

Each motor could read the battery for itself. Work out what goes wrong when two motors read a sagging
rail a few milliseconds apart while being handed the same commanded voltage.

<Solution>

They get different divisors, so the same 6 V request becomes two different powers — and on a
drivetrain that is a steering error the controller never asked for, appearing only when the pack is
sagging, which is exactly when you are pushing. One reading per loop makes the whole robot agree on
what the battery is doing.

</Solution>

</Exercise>

<Exercise title="The sign of the effect">

You want 6 volts. At a fresh 13.0 V the power is 0.46; at a sagging 11.0 V it is 0.55. Explain why
commanding a *larger* number is the correct response to a *weaker* battery.

<Solution>

`setPower` is a duty cycle, not a voltage — it is the fraction of the rail you are asking for. A
weaker rail means a larger fraction is needed to reach the same actual voltage at the motor. Without
the division, the same `0.46` would deliver 6.0 V on a fresh pack and 5.06 V on a sagging one, and
every gain you tuned would quietly drift over the match.

</Solution>

</Exercise>

In FRC, the motor controllers do it for you with `enableVoltageCompensation(nominalVolts)` — set it once
and command in volts with `setVoltage(...)` thereafter. The idea is identical: stop thinking in
unitless "power" and start thinking in volts the battery may or may not be able to supply.

<Callout type="tip" title="Compensation is not a brownout cure.">
Holding the *command* constant as voltage drops means drawing *more current* to do it — which sags the
pack further. Compensation keeps behavior consistent within the battery's ability; it cannot create energy
that isn't there. If you're browning out, the fix is in the [motor model](./how-a-motor-works.mdx) (you're
living too near stall), not here.
</Callout>

</Deploy>

<Admonition type="warning" title="Hardware reality: validate the voltage reading">

Battery voltage spikes downward sharply under sudden load (a drivetrain slamming to full power), so a raw
reading can momentarily read very low and make your compensation over-command. Reject nonfinite or
physically implausible readings as the snippet does. If measurement noise requires it, use only a very
light [low-pass filter](../signal-processing/low-pass.mdx): genuine bus sag is the value the command must
respect, so heavy smoothing would hide the condition. Read voltage once per loop rather than per
motor — the sensor call has real latency.

</Admonition>

<Admonition type="info" title="Sources & references">

- [WPILib: Feedforward Control](https://docs.wpilib.org/en/stable/docs/software/advanced-controls/controllers/feedforward.html) -
  competition-robotics reference for commanding mechanisms in volts instead of relying on unitless power.
- Game Manual 0, [Motor Power](https://gm0.org/en/latest/docs/power-and-electronics/motor-guide/motor-power.html) -
  FTC motor current and stall context, and the 12 V nominal basis for motor performance curves. (This page
  does not itself cover battery voltage sag; no FTC-specific manufacturer source with sourced voltage-sag
  figures was found, so the sag numbers above are presented as illustrative, not sourced.)
- [How a Motor Works](./how-a-motor-works.mdx) - Control Lab's motor-model lesson explaining why voltage,
  current, speed, and torque are coupled.

</Admonition>
