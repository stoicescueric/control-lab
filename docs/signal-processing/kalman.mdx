---
title: Kalman Filter
description: The optimal linear state estimator under linear-Gaussian assumptions — predict then correct, blending model and measurement by covariance while tracking uncertainty.
sidebar_position: 7
tags: [filter, kalman, state-estimation, sensor-fusion]
---

import Kalman from '@site/src/components/simulations/signal-processing/Kalman';
import {KalmanGainExplorer} from '@site/src/components/simulations/signal-processing/FilterMathDemos';
import useBaseUrl from '@docusaurus/useBaseUrl';

# Kalman Filter

<Difficulty level="Hard" />

<Abstract>
The Kalman filter is the optimal linear state estimator under the usual linear-Gaussian assumptions:
it predicts the state from a motion model, then corrects with each measurement in the proportion that
minimises variance, all while tracking its own uncertainty. This lesson derives that predict-correct
cycle and the gain that decides how far to trust the model versus the measurement.
</Abstract>

<Admonition type="note" title="Prerequisites and a starting path">

You do **not** need a probability course before starting. Begin with
[Signals & Noise](./signals.mdx) to learn mean, standard deviation, and what a sensor measurement
actually contains; then read the [Complementary Filter](./complementary.mdx) for the predict/correct
idea with a fixed blend. Here, a **Gaussian** is simply the bell-shaped uncertainty model summarized by
its mean and variance. The matrix form later in the lesson is optional depth; first make the one-state
predict/correct loop work.

**Math used:** mean, variance, and a bell-shaped Gaussian in the scalar route; matrix multiplication and
transpose in the matrix route. Treat probability, variance, and covariance as required background for
the full matrix derivation. The Extended Kalman Filter section additionally uses **partial derivatives** and
**Jacobians**; it is optional after the linear filter.

</Admonition>

The Kalman filter is the optimal linear state estimator when the model is linear, the noise is
Gaussian, the covariances are honest, and that noise is **zero-mean and uncorrelated from loop to
loop**: given a motion model and noisy measurements, it produces the minimum-variance estimate of the
true state, and tracks its own uncertainty while doing so. It is the principled successor to the
hand-tuned complementary filter.

That last assumption is the one real sensors break. A camera with a bent mount or a miscalibrated tag
carries a *constant offset*, not zero-mean noise — changing $R$ alone does not estimate that bias.
Calibrate the sensor, or explicitly model an observable bias state with enough independent information
to estimate it. The unmodified filter can converge confidently onto a biased answer.

<Admonition type="info" title="Where this leads: the Extended Kalman Filter">

Kou and Haggenmiller's FTC-focused paper, *Extended Kalman Filter State Estimation for Autonomous
Competition Robots*, starts from the same predict-correct loop and extends it to a mecanum robot whose
body-to-field motion contains $\sin\theta$ and $\cos\theta$. One way to handle that nonlinearity is an
**Extended Kalman Filter (EKF)**: predict with nonlinear functions, then use their local Jacobians to
propagate uncertainty. Finish the linear filter first; the EKF bridge later in this lesson shows exactly
what changes.

</Admonition>

<Problem>

## A fixed blend is not the optimal blend

The complementary filter blends two sources with a *constant* weight $\alpha$. But the right amount
to trust a measurement is not constant — it depends on how uncertain your prediction currently is
versus how noisy the sensor is. On a robot this is concrete: dead reckoning from a pose source verified
against the [Pose Estimation Contract](../path-following/pose-estimation-contract.mdx) is smooth but
drifts, while an absolute fix (an AprilTag pose, a beacon) is noisy and intermittent. Early after a
correction the model is trustworthy; after a long blind stretch it is not. A fixed weight cannot
express that, and it cannot report how confident the resulting estimate is.

</Problem>

<KalmanGainExplorer />

This Desmos-style graph is the Kalman filter in one dimension. The blue curve is the model's
prediction, the pink curve is the sensor measurement, and the green curve is the corrected estimate.
Wide curves mean high variance: the value could plausibly be far from the center. Narrow curves mean
low variance: that source is confident. Move the variance sliders and watch the corrected estimate
move toward the narrower curve.

## Core checkpoint: one persistent scalar filter

Start with one quantity, such as an $x$ coordinate. The random-walk model below predicts by adding the
measured motion increment `delta`, adds process variance `Q`, then corrects whenever an absolute
measurement arrives. `xHat` and `P` are fields, so every loop starts from the previous corrected result.

<JavaCode league="FTC" label="Persistent scalar predict/correct loop">{`private double xHat;   // the estimate; start at your best guess
private double P;      // its variance; start LARGE if the guess is poor
private double Q;      // process noise: how far truth drifts per loop
private double R;      // measurement noise: how much the sensor lies

// Every loop, from a measured displacement.
void predict(double delta) {
    xHat += delta;
    P += Q;
}

// Only when a validated measurement exists.
void correct(double z) {
    double K = P / (P + R);      // the Kalman gain
    xHat += K * (z - xHat);      // (z - xHat) is the innovation
    P = (1 - K) * P;
}`}</JavaCode>

Starting from $\hat x_0 = 0$ and $P_0 = 4$, with `delta = 1`, `Q = 1`, `R = 4`, and measurements
`1.2, 2.1, 2.9`, three complete loops are:

| Loop | prediction $\hat x^-$ | gain $K$ | corrected $\hat x$ | corrected $P$ |
|---:|---:|---:|---:|---:|
| 1 | 1.000 | 0.556 | 1.111 | 2.222 |
| 2 | 2.111 | 0.446 | 2.106 | 1.785 |
| 3 | 3.106 | 0.410 | 3.022 | 1.642 |

If you can trace `predict` then `correct` and explain why a larger `P` produces a larger gain, the required
artifact is complete. The external [Pose Estimation Contract](../path-following/pose-estimation-contract.mdx)
can supply an example motion increment, but this scalar filter is not itself a localization system.

<Exercise title="The two extremes of the gain">

Everything interesting is in $K = P/(P+R)$. Work out what $\hat x$ becomes when $P$ is far larger than
$R$, and again when $R$ is far larger than $P$. Then take the second case further: an FTC robot might
run `predict` at 50 Hz off odometry but only `correct` when an AprilTag comes into view. Trace what $P$
does across a long blind stretch, and what that means for the gain on the next correction.

<Solution>

With $P \gg R$ the gain approaches 1 and $\hat x \to z$: the filter throws away its own estimate and
believes the sensor. With $R \gg P$ the gain approaches 0 and $\hat x$ barely moves: the measurement is
ignored. Between corrections `predict` adds $Q$ every loop, so $P$ grows without bound. The longer the
robot goes without a tag, the larger $P$ is when one finally appears, and the harder the filter snaps to
it. That is the behavior you want, and it is also why a tag seen at a bad angle can yank the pose.

</Solution>

</Exercise>

<Exercise title="Where alpha came from">

Put this beside the low-pass filter you already wrote. Both compute
`estimate + gain * (measurement - estimate)`; the only difference is where the gain comes from. You
picked $\alpha$ by hand, and $K$ is derived. Work out what $K$ *converges to* over many loops when $Q$
and $R$ are held constant: solve the fixed point of $P^- = (1-K)P^- + Q$. Then set $Q = 0$ and redo it.

<Solution>

With $Q$ and $R$ constant the gain settles on a constant. Writing $p$ for the steady-state $P^-$, the
fixed point is $p = \frac{Rp}{p+R} + Q$, or $p^2 - Qp - QR = 0$, so

$$
p = \frac{Q + \sqrt{Q^2 + 4QR}}{2}, \qquad K_\infty = \frac{p}{p + R}.
$$

For the $Q = 1$, $R = 4$ used above that is $p = 2.562$ and $K_\infty = 0.390$ — exactly where the
three-loop table is heading (0.556, 0.446, 0.410). **That is where $\alpha$ came from.** A settled
scalar random-walk Kalman filter with $A = H = 1$ *is* a first-order low-pass filter; its blend factor was computed
from your noise estimates instead of guessed.

Now set $Q = 0$. Nothing re-inflates $P$ after each correction shrinks it, so $1/P_k = 1/P_0 + k/R$ and
$K \to 0$: each new measurement receives progressively less weight. That is appropriate for a truly
constant state, but makes the filter increasingly slow to follow unmodeled changes. The gain approaches
zero asymptotically; it does not stop updating after a finite number of measurements. A positive $Q$
keeps a nonzero steady gain in this scalar model, although an underestimated $Q$ can still cause lag.

</Solution>

</Exercise>

Set $R$ from the noise standard deviation you measured in
[Signals and noise](./signals.mdx) — it is that number squared. $Q$ is the honest guess of the two:
start with the drift you can actually observe between corrections and tune from there.

<Theory>

## Optional continuation: many states and the optimal gain

The filter maintains a state estimate $\hat{x}$ and a covariance $P$ (its uncertainty), and repeats
two steps. **Predict** advances the state through the motion model; process noise $Q$ adds uncertainty:

$$
\hat{x}^- = A\hat{x} + Bu, \qquad P^- = A P A^\top + Q
$$

<EquationLegend
  items={[
    {symbol: 'the superscript minus', meaning: 'a PRIOR: after prediction, before any measurement has corrected it. Losing track of which values carry the minus is the most common way to misread these equations'},
    {symbol: 'APA\u1d40', meaning: 'how the existing uncertainty is reshaped by the motion. Q is then ADDED on top, because the model is never perfect. Whether the total actually grows depends on A \u2014 see the note below the equation'},
  ]}
/>

<EquationLegend
  title="Prediction symbols"
  items={[
    {symbol: 'x-hat', meaning: 'the filter estimate of the state, such as position or velocity'},
    {symbol: 'A', meaning: 'state-transition matrix: how the state naturally advances one loop'},
    {symbol: 'B', meaning: 'input matrix: how the control command changes the state'},
    {symbol: 'u', meaning: 'control input, such as commanded velocity or acceleration'},
    {symbol: 'P', meaning: 'state covariance: uncertainty attached to the estimate'},
    {symbol: 'Q', meaning: 'process-noise covariance: uncertainty added because the model is imperfect'},
  ]}
/>

Here $\hat{x}$ is the current best estimate of the state, such as position or velocity; $A$ is the
state-transition matrix that advances the state by one loop; $u$ is the control input; $B$ maps that
control input into state change; $P$ is the covariance matrix, meaning the uncertainty attached to
the estimate; and $Q$ is process noise, the uncertainty added because the model is never perfect. Total
covariance does not have to grow on every prediction: a contracting $A$ can shrink $APA^\top$ by more than
$Q$ adds.

The paper writes this as the prediction model $x_{a,t}=Ax_{a,t-1}+Bu_t+q_t$, where $q_t$ is
Gaussian process noise with covariance $Q$.

If the state has only one number, such as "robot x-position," covariance reduces to ordinary
variance. In that scalar case the update gain becomes:

$$
K = \frac{P^-}{P^- + R}
$$

<EquationLegend
  items={[
    {symbol: 'P\u207b / (P\u207b + R)', meaning: 'the entire idea in one fraction. P\u207b is how much you doubt yourself, R how much you doubt the sensor. K is your doubt as a share of the total, so it always lands between 0 and 1'},
  ]}
/>

That formula is the whole intuition. If $P^-$ is large, the prediction is uncertain, so $K$ moves
toward $1$ and the measurement gets trusted. If $R$ is large, the sensor is noisy, so $K$ moves
toward $0$ and the prediction stays mostly unchanged. The matrix equation is the same idea, except
the filter can track uncertainty across several coupled state variables at once.

**Update** folds in a measurement $z$ and *shrinks* the uncertainty, weighted by the **Kalman
gain** $K$:

$$
K = P^- H^\top (H P^- H^\top + R)^{-1}, \qquad
\hat{x} = \hat{x}^- + K\,(z - H\hat{x}^-), \qquad
P = (I - KH)P^-
$$

<EquationLegend
  items={[
    {symbol: 'H', meaning: 'the measurement matrix: it maps a state into what a sensor would report. It is needed because the state and the measurement often are not the same quantity'},
    {symbol: 'I \u2212 KH', meaning: 'the shrink factor on the covariance. A large K means the measurement was trusted, so more uncertainty is removed'},
    {symbol: 'the inverse', meaning: 'in the scalar case this is ordinary division, giving exactly the simpler gain shown above'},
  ]}
/>

<EquationLegend
  title="Update symbols"
  items={[
    {symbol: 'z', meaning: 'sensor measurement received this loop'},
    {symbol: 'H', meaning: 'measurement matrix: maps state space into sensor space'},
    {symbol: 'R', meaning: 'measurement-noise covariance: how noisy the sensor is'},
    {symbol: 'K', meaning: 'Kalman gain: how strongly the innovation should correct the prediction'},
    {symbol: 'I', meaning: 'identity matrix, used to shrink covariance after the correction'},
  ]}
/>

In this update, $z$ is the sensor measurement, $H$ maps the state into whatever the sensor can
observe, $R$ is measurement-noise covariance, and $I$ is the identity matrix. The term
$(z-H\hat{x}^-)$ is the innovation: the part of the measurement the model did not already predict.

Read the update in three pieces:

$$
\text{innovation} = z - H\hat{x}^-
$$

$$
\text{correction} = K \cdot \text{innovation}
$$

$$
\text{new estimate} = \text{prediction} + \text{correction}
$$

The innovation answers, "How surprised was the filter by this sensor reading?" The Kalman gain
answers, "How much of that surprise should we believe?" The covariance update answers, "After
seeing this measurement, how much uncertainty remains?"

The update is the low-pass formula you already know — *estimate = prediction + K·(measurement −
prediction)* — except $K$ is **derived optimally** from the prediction covariance $P^-$ and the
measurement noise $R$, every step:

- noisy sensor (large $R$) → small $K$ → trust the model, stay smooth;
- shaky prediction (large $P^-$) → large $K$ → jump toward the measurement.

In the demo, a target follows a smooth path reported only as noisy blips. The filter recovers a
smooth estimate plus an uncertainty bubble. The sensor slider sets standard deviation $\sigma$
in simulation distance units and sets the filter's $R = \sigma^2$ to match. It changes both the
measurements and the filter's assumption, so this is not a test of an incorrectly specified $R$.
The other slider sets assumed acceleration variance $q$. For each position/velocity axis, the demo
uses $Q = q\begin{bmatrix}\Delta t^4/4 & \Delta t^3/2 \\\Delta t^3/2 & \Delta t^2\end{bmatrix}$,
with $\Delta t = 0.06$ s: random acceleration held constant over one interval. The target follows a
deterministic curved path, so this process model is an approximation.
The bubble radius is twice the estimated position standard deviation (with a 4-pixel display minimum);
it illustrates model uncertainty, not a guaranteed containment region or a 95% two-dimensional bound.

<Kalman />

<Callout type="warn" title="Q and R are the tuning.">
Set <code>Q</code> very low and the estimate lags on sharp turns: you have told the filter its motion
model is nearly perfect, so it over-believes its own prediction. Set it high and it chases every blip.
Note the direction — <code>Q</code> is how much you <strong>distrust</strong> the model, so raising it
makes the filter trust the sensor more, not less. Choosing <code>Q</code> and <code>R</code> to match
the real process and sensor noise is the engineering of applying a Kalman filter.
</Callout>

</Theory>

## Introduction to the Extended Kalman Filter

<Admonition type="note" title="Extra prerequisite for the EKF extension">

The next section needs [Calculus](../preface/why-math-matters/calculus.mdx) for partial derivatives and
[Linear Algebra](../preface/why-math-matters/linear-algebra.mdx) for Jacobian matrices. Stop here if the
linear Kalman loop is your goal; it is complete and practical on its own.

</Admonition>

A linear Kalman filter predicts with matrices $A$ and $B$ and predicts a measurement with matrix $H$.
An EKF allows nonlinear functions instead:

$$
\hat{\mathbf x}^{-}_k=f(\hat{\mathbf x}_{k-1},\mathbf u_k),
\qquad
\hat{\mathbf z}_k=h(\hat{\mathbf x}^{-}_k).
$$

The mean follows the real nonlinear motion and sensor functions. Covariance still needs a linear map,
so the EKF recalculates two Jacobians at the current estimate:

$$
F_k=\left.\frac{\partial f}{\partial\mathbf x}\right|_{\hat{\mathbf x}_{k-1}},
\qquad
H_k=\left.\frac{\partial h}{\partial\mathbf x}\right|_{\hat{\mathbf x}^{-}_k}.
$$

Then the familiar equations return with $F_k$ and $H_k$:

$$
P^-_k=F_kP_{k-1}F_k^\top+Q,
$$

$$
K_k=P^-_kH_k^\top(H_kP^-_kH_k^\top+R)^{-1},
$$

$$
\hat{\mathbf x}_k=\hat{\mathbf x}^{-}_k+K_k(\mathbf z_k-h(\hat{\mathbf x}^{-}_k)).
$$

The attached paper's example state contains field position and heading plus body-frame velocities:

$$
\mathbf x=[x,\ y,\ \theta,\ v_x^{body},\ v_y^{body},\ \omega]^\top.
$$

Body velocity is rotated into the field frame during prediction, making $f$ nonlinear. Odometry supplies
frequent velocity measurements, while landmark detections supply intermittent absolute position. The EKF
can predict through the gaps and reduce accumulated drift when a valid landmark returns.

<Callout type="warn" title="An EKF is a local approximation, not a magic nonlinear optimum.">

The Jacobians are tangent-plane approximations around the current estimate. Large time steps, a poor
initial estimate, severe nonlinearity, biased sensors, or dishonest $Q$ and $R$ can make the covariance
misleading or make the filter diverge. The paper reports that larger update intervals degrade its
linearization and notes that body-frame velocities are less nonlinear than global velocities for the
modeled mecanum motion. Recompute Jacobians at every step, keep timestamps honest, wrap angles in the
innovation, and reject invalid vision before correction.

</Callout>

The conceptual change is compact:

| Linear KF | Extended KF |
|---|---|
| $A\mathbf x+B\mathbf u$ | nonlinear $f(\mathbf x,\mathbf u)$ |
| $H\mathbf x$ | nonlinear $h(\mathbf x)$ |
| fixed or scheduled $A,H$ | Jacobians $F_k,H_k$ at the current estimate |
| optimal under linear-Gaussian assumptions | approximate for nonlinear systems |

For an FTC implementation, use a maintained pose-estimation library unless deriving and validating every
frame, Jacobian, covariance, timestamp, and reset rule is itself the project. Read the attached paper after
you can trace the matrix KF equations above; its simulation model, covariance choices, and fusion results
make the EKF transition concrete.

<Deploy>

## On a robot: retain the scalar state

This example consumes motion from an external pose source. Verify its frame, units, update cadence, and
freshness with the [Pose Estimation Contract](../path-following/pose-estimation-contract.mdx) first.

Full matrix pose estimators should normally come from a maintained robotics library. For the scalar class
above, keep one `ScalarKalman` object for the lifetime of the OpMode: call `predict(deltaX)` once from the
current coherent motion sample, then call `correct(tagX)` only when vision passes timestamp, calibration,
and geometry checks. Never reconstruct the object inside the loop, because that discards the last
corrected estimate and covariance.

<Callout type="tip" title="It extracts signal from noise.">
In the demo the Kalman error is typically a fraction of the raw blip error — genuine information
recovery, not just smoothing. That is why it underpins GPS, drone, and robot localisation.
</Callout>

Full field localisation is nonlinear, so take the maintained estimator's absolute pose only after
verifying it against the [Pose Estimation Contract](../path-following/pose-estimation-contract.mdx).
Use this lesson's scalar filter for one-dimensional signals you intentionally fuse yourself.

</Deploy>

<Admonition type="info" title="Sources and further reading">

- Kou, E., & Haggenmiller, A. (2023), *Extended Kalman Filter State Estimation for Autonomous Competition Robots* — <a href={useBaseUrl('/papers/extended-kalman-filter-competition-robots.pdf')}>download the attached paper</a> or open the [arXiv record](https://arxiv.org/abs/2310.04459). Read §§III-B and IV first for the Jacobian transition and the FTC mecanum state model, then §§VI–VII for limitations and next steps.
- [WPILib State Observers and Pose Estimators](https://docs.wpilib.org/en/stable/docs/software/advanced-controls/state-space/state-space-observers.html) — maintained FRC implementations and the practical relationship between observers and pose estimators.
- R. E. Kalman, [*A New Approach to Linear Filtering and Prediction Problems*](https://doi.org/10.1115/1.3662552) — the original linear filter after the scalar intuition in this lesson is comfortable.

</Admonition>
