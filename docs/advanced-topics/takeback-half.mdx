---
title: Take-Back-Half
description: The one-line upgrade to bang-bang flywheel control — integrate the error, and each time it changes sign give back half the accumulated drive — converging to a steady, ripple-free hold while keeping bang-bang's fast recovery after a shot.
sidebar_position: 3
tags: [takeback-half, tbh, bang-bang, flywheel, control, advanced]
---

import BangBang from '@site/src/components/simulations/control-theory/BangBang';

# Take-Back-Half

<Difficulty level="Medium" />

<Abstract>
Bang-bang recovers a flywheel's speed fast but chatters and never truly settles. <strong>Take-back-half</strong>
(TBH) is a tiny modification — integrate the error like an integrator, and when the error changes sign, give
back half the accumulated drive — that converges to a steady, ripple-free hold while keeping bang-bang's
fast recovery after a shot. One gain, no derivative, no windup.
</Abstract>

<Admonition type="note" title="Prerequisite">

This page extends [**Bang-Bang Control**](../control-theory/bang-bang.mdx). It assumes you know why a
high-inertia flywheel suits an on/off law and why plain bang-bang chatters in a limit cycle around the
setpoint.

</Admonition>

<Theory title="Settle without giving up recovery">

Take-back-half keeps an accumulating drive like an integrator, but adds one trick. Each loop, integrate the
error into the drive. When the error **crosses zero**: the moment you pass the setpoint — you have
integrated too far, so "take back half": set the drive to the average of its current value and its value
at the *previous* crossing. Successive crossings bracket the true holding drive and converge onto it.

$$
\begin{gathered}
\text{drive} \mathrel{+}= g\,e\,\Delta t \\[3pt]
\text{on a sign change of } e:\quad
\text{drive} \leftarrow \tfrac12\big(\text{drive} + \text{drive}_\text{last-cross}\big)
\end{gathered}
$$

<EquationLegend
  items={[
    {symbol: 'drive', meaning: 'the running output, from 0 to 1. It is pure integrator: nothing resets it except the take-back rule'},
    {symbol: 'g', meaning: 'the single tuning gain. TBH exists because one gain is easier to get right than three', unit: 'power per (error \u00b7 s)'},
    {symbol: 'drive_last-cross', meaning: 'the value drive held the PREVIOUS time the error changed sign. Averaging against it is what brackets the true holding value'},
    {symbol: 'a sign change of e', meaning: 'the moment the mechanism crosses its target. Each crossing halves the remaining oscillation, which is why successive crossings converge'},
  ]}
/>

The result (green below) recovers nearly as fast as bang-bang on the first approach, then quickly stops
oscillating and holds the speed flat. One gain $g$, no derivative, no windup to manage. Run the demo:
bang-bang chatters around the line forever; TBH locks on.

<BangBang />

<Callout type="idea" title="Why take back half?">
At each zero crossing the true steady drive lies somewhere between the current over-driven value and the
last under-driven one. Halving splits the difference — a bisection search on the holding voltage that
converges geometrically. It is the cheapest possible way to find the steady command without a model.
</Callout>

</Theory>

<Deploy title="Take-back-half on the Control Hub">

<Pseudocode label="Takeback-half velocity controller">{`TAKE-BACK-HALF      a velocity controller with ONE gain

  state:  drive           <- 0     the current output
          drive_at_zero   <- 0     the output at the last error sign change
          last_error      <- none

  update(measured, target, dt):
      error <- target - measured

      drive <- drive + GAIN * error * max(0, dt)      # integrate
      clamp drive into [0, 1]

      if error is not exactly zero:
          if last_error exists and sign(error) differs from sign(last_error):
              # we just crossed the target. take back half.
              drive         <- (drive + drive_at_zero) / 2
              drive_at_zero <- drive
          last_error <- error
          # a zero error is skipped so it cannot fake a second crossing

      return drive

# TBH is a pure integrator with one trick: whenever the error changes sign,
# it jumps the output halfway back toward whatever it was at the PREVIOUS
# crossing. Work out why that converges. Sketch a flywheel overshooting,
# crossing, undershooting, crossing again, and mark drive at each crossing.
# The oscillation halves each time.
#
# Work out why this suits a flywheel where a PID struggles. A flywheel has
# huge inertia and asymmetric authority: the motor can push hard but can
# only coast down. What does an integrator with anti-windup do here, and how
# many gains did you have to tune to get it right? TBH has one.
#
# Work out why drive is clamped to [0, 1] rather than [-1, 1].
#
# Look up: "take back half controller", originally from VEX flywheel
#          control. Compare its step response to a tuned PI on the same
#          mechanism.
`}</Pseudocode>

Seed `driveAtZero` with a [feedforward](../control-theory/feedforward.mdx) estimate ($k_V \cdot v_\text{target}$)
and TBH converges in a single oscillation instead of several — the two techniques compose nicely.

</Deploy>

<Admonition type="tip" title="When to reach for which">

Use **[bang-bang](../control-theory/bang-bang.mdx)** when recovery speed is everything and the plant is
high-inertia (a shooter you re-spin between shots). Use **take-back-half** when you also need a steady,
repeatable speed (consistent shot distance). Use **[PID](../control-theory/pid.mdx) + feedforward** when
you need both tight holding *and* controlled acceleration, or the mechanism isn't a flywheel.

</Admonition>

<Admonition type="info" title="Sources & references">

- [Purdue SIGBots (BLRS) wiki - Take-Back-Half (TBH) controller](https://wiki.purduesigbots.com/software/control-algorithms/take-back-half-tbh-controller) -
  description source for this lesson; credits Pearman, J. (2015), "Flywheel velocity control," VexForum,
  and Harper, C. S. (2011), master's thesis, Mississippi State University, for the algorithm as applied to
  FRC/VEX flywheel control.
- Woodward, S. (2005), "Take-back-half technique tempers temperature-control-loop overshoot," EDN
  ("Design Ideas") - the original bisection technique, developed for temperature control and later adapted
  by the FRC/VEX community for flywheel speed control.

This lesson is an independent reimplementation.

</Admonition>
